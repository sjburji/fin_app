<%= form_for(@geography) do |f| %>
  <% if @geography.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@geography.errors.count, "error") %> prohibited this geography from being saved:</h2>

      <ul>
      <% @geography.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :created_at, :value => Time.zone.today %>
  <%= f.hidden_field :updated_at, :value => Time.zone.today %>
  <%= f.hidden_field :created_by, :value => current_user.id.to_s %>
  <%= f.hidden_field :modified_by, :value => current_user.id.to_s %>
  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <span class="span-3"><%= f.label :category %></span>
    <span class="span-3"><%= f.radio_button "category", 'City', {:checked => true} %>  City</span>
    <span class="span-3"><%= f.radio_button "category", 'State' %>  State</span>
    <span class="span-3"><%= f.radio_button "category", 'Country' %>  Country</span>
  </div>
  <br/><br/>

  <div class="actions">    
    <%= f.submit %>
  </div>
<% end %>
